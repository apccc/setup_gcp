#!/bin/bash

#script for scripts to quickly get the mysql root password - if they have access to this then they're already in too far anyway :(

source ~/setup/settings/core.sh

echo "$MYSQL_ROOT_PW"

exit 0
